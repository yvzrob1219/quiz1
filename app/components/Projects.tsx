import React from "react";

const Projects = () => {
	return (
		<div id="projects" className={'flex flex-col min-h-screen align-middle w-full justify-center items-center'}>
			<h2 className={'top-0'}>Projects</h2>
      <p> nothing to see here yet :D</p>
		</div>
	);
};

export default Projects;
